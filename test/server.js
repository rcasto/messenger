var assert = require('assert');

/* Modules being tested */
var server = require('../server.js');

console.log(server);

describe('Server', function() {
    it('can initialize server', function() {
        
    });
});